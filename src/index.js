/**
 * Galascribe's executable schema catalog.
 *
 * Contract exports are introduced only by the S0 tasks that materialize them.
 * The scaffold intentionally defines no provisional public API.
 *
 * @module
 */

export {};
